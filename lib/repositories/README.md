# Repositories

This directory contains repository classes that handle data access and Firebase operations.

## Planned Repositories:
- `auth_repository.dart` - Firebase Authentication operations
- `user_repository.dart` - User CRUD operations
- `case_repository.dart` - Case CRUD operations and filtering
- `dashboard_repository.dart` - Dashboard metrics and analytics
