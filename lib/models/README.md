# Models

This directory contains data models for the TB Notification Tracker application.

## Planned Models:
- `user_model.dart` - User data model with role enum and validation
- `case_model.dart` - Case data model with status/gender enums and validation
